pipeline {

    agent any

    stages {

        stage("Pull Code") {
            steps {
                echo "Pulling code from GitHub"
            }
        }

        stage("Build") {
            steps {
                echo "Building package"
            }
        }

        stage("Test") {
            steps {
                echo "Testing package"
            }
        }

    }

}
